# Local release-signing credentials — NEVER commit this file.
# Copy to keystore.properties and fill in. CI uses KEYSTORE_* env secrets instead.
storeFile=/absolute/path/to/chaya-release.keystore
storePassword=CHANGE_ME
keyAlias=chaya
keyPassword=CHANGE_ME
